services:
  django_backend:
    build: ./
    container_name: face_recognition_django
    volumes:
      - ./:/code  # Maps the host's directory to /code in the container
      - ./static:/code/static
    ports:
      - "8002:8000"
    restart: always
